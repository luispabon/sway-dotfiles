# Ubuntu's default audio powersave is too aggressive (around 10 seconds)
# causing tons of popping sounds when soundcard
# USB audio is done at the install.sh script via tlp config
options snd_hda_intel power_save=600
